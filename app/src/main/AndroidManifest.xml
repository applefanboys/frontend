<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.StocksApp">
        <receiver
            android:name=".ui.main.NewsAlarmReceiver"
            android:exported="false" />


        <activity
            android:name=".ui.main.WebViewActivity"
            android:exported="false" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.main.AudioNewsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.ResetPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.ResetPasswordActivity"
            android:exported="false" />



        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.ResetPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.ResetNewPasswordActivity"
            android:exported="true">

            <!-- 딥링크용 인텐트 필터 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- shoteconomy://reset-password?token=... 를 받기 위한 설정 -->
                <data
                    android:scheme="shorteconomy"
                    android:host="reset-password" />
            </intent-filter>
        </activity>


    </application>


</manifest>
